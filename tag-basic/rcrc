EXCLUDES="README.rst LICENSE unlinked dotfiles.code-workspace"
UNDOTTED="Desktop backup"
DOTFILES_DIRS="~/.dotfiles"

# load user specific .rcrc
rcrc_user="$HOME/.rcrc.user"
if test -r $rcrc_user -a -f $rcrc_user;then source $rcrc_user; fi
